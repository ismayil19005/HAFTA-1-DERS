/*using System;

class Program
{
    static void Main()
    {
        
        Console.Write("Bir sayı girin: ");
        int sayi = Convert.ToInt32(Console.ReadLine());
        if (sayi % 2 == 0)
        {
            Console.WriteLine("Girilen sayı çifttir.");
        }
        else
        {
            Console.WriteLine("Girilen sayı tektir.");
        }
    }
}
*/
using System;

/*class Program
{
    static void Main()
    {
        for (int i = 1; i <= 10; i++)
        {
            Console.WriteLine(i.ToString());
        }
    }
}
*/
using System;

/*class Program
{
    static void Main()
    {
        string dogruKullaniciAdi = "admin";
        string dogruSifre = "Admin_32453@";
        Console.Write("Kullanıcı adını girin: ");
        string kullaniciAdi = Console.ReadLine();
        Console.Write("Şifreyi girin: ");
        string sifre = Console.ReadLine();
        if (kullaniciAdi == dogruKullaniciAdi && sifre == dogruSifre)
        {
            Console.WriteLine("Giriş başarılı.");
        }
        else
        {
            Console.WriteLine("Giriş başarısız. Kullanıcı adı veya şifre yanlış.");
        }
    }
}
*/
using System;

/*class Program
{
    static void Main()
    {
        Console.Write("Sıcaklık değerini girin: ");
        int sicaklik = Convert.ToInt32(Console.ReadLine());
        if (sicaklik >= 10 && sicaklik < 20)
        {
            Console.WriteLine("Sıcaklık durumu: Normal");
        }
        else if (sicaklik >= 20 && sicaklik < 30)
        {
            Console.WriteLine("Sıcaklık durumu: Sıcak");
        }
        else if (sicaklik >= 30 && sicaklik < 40)
        {
            Console.WriteLine("Sıcaklık durumu: Çok Sıcak");
        }
        else
        {
            Console.WriteLine("Sıcaklık durumu: Geçersiz değer. Lütfen 10-40 arasında bir sıcaklık girin.");
        }
    }
}
*/
